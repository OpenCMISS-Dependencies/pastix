diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 9c03b93..3b254fa 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -24,7 +24,6 @@ macro(IMPORT WHAT)
         symbol/src common/src order/src sopalin/src perf/src
         fax/src blend/src perf/src/num_recipes kass/src
         PUBLIC
-        ${MPI_C_INCLUDE_PATH} ${MPI_Fortran_INCLUDE_PATH}
         $<$<BOOL:${USE_PTSCOTCH}>:
             $<TARGET_PROPERTY:ptscotch,INTERFACE_INCLUDE_DIRECTORIES>>
         $<$<BOOL:${USE_PTSCOTCH}>:
@@ -74,7 +73,6 @@ macro(IMPORT WHAT)
             target_include_directories(${lib} PRIVATE 
                 order/src common/src blend/src symbol/src
                 kass/src fax/src perf/src
-                ${MPI_C_INCLUDE_PATH} ${MPI_Fortran_INCLUDE_PATH}
                 $<$<BOOL:${USE_PTSCOTCH}>:
                     $<TARGET_PROPERTY:ptscotch,INTERFACE_INCLUDE_DIRECTORIES>>
                 $<$<BOOL:${USE_PTSCOTCH}>:
@@ -151,9 +149,7 @@ if (USE_THREADS)
     #endif()
 endif()
 target_link_libraries(pastix PRIVATE blas PUBLIC ${GRAPH_LIBS})
-if (NOT CMAKE_C_COMPILER STREQUAL MPI_C_COMPILER)
-    target_link_libraries(pastix PUBLIC $<BUILD_INTERFACE:mpi>)
-endif()
+target_link_libraries(pastix PUBLIC $<BUILD_INTERFACE:mpi-c>)
     
 target_include_directories(pastix 
     PRIVATE common/src symbol/src order/src blend/src sopalin/src
@@ -171,4 +167,4 @@ add_subdirectory(matrix_drivers/src)
 
 if (BUILD_TESTS)
     add_subdirectory(example/src)
-endif()
\ No newline at end of file
+endif()
diff --git a/src/example/src/CMakeLists.txt b/src/example/src/CMakeLists.txt
index c62f4f4..19c54fc 100644
--- a/src/example/src/CMakeLists.txt
+++ b/src/example/src/CMakeLists.txt
@@ -1,8 +1,5 @@
-# Build examples.
-if (MPI_FOUND)
-    include_directories(${MPI_Fortran_INCLUDE_PATH} ${MPI_CXX_INCLUDE_PATH} ${MPI_C_INCLUDE_PATH})
-endif()
 
+# Build examples.
 macro(ADDEXEC name src prec)
     #message(STATUS "adding executable ${name} with incdir: ${PASTIX_INCLUDE_DIR_PREC}")
     add_executable(${name} ${src})
@@ -63,14 +60,15 @@ if (MPI_FOUND)
     add_dependencies(futils ${NEUTRAL_PRECISION}matrix_driver)
     target_include_directories(futils PRIVATE
         ${PASTIX_INCLUDE_DIR_PREC}
+        ${MPI_Fortran_INCLUDE_PATH}
     ) 
     macro(ADDEXECF name src)
         add_executable(${name} ${src} $<TARGET_OBJECTS:futils>)
-        target_link_libraries(${name} pastix ${NEUTRAL_PRECISION}matrix_driver)
+        target_link_libraries(${name} pastix ${NEUTRAL_PRECISION}matrix_driver mpi-fortran)
         target_include_directories(${name} PRIVATE
             ${PASTIX_INCLUDE_DIR_PREC}
         )    
     endmacro()
     ADDEXECF(fstep-by-step fstep-by-step.F90)
     ADDEXECF(fsimple fsimple.F90)
-endif()
\ No newline at end of file
+endif()
diff --git a/src/sopalin/src/CMakeLists.txt b/src/sopalin/src/CMakeLists.txt
index 080e6d3..a1413eb 100644
--- a/src/sopalin/src/CMakeLists.txt
+++ b/src/sopalin/src/CMakeLists.txt
@@ -70,7 +70,7 @@ if (BUILD_TESTS)
     endmacro()
     macro(ADDTEST_MPI basename np)
         add_executable(${basename} ${basename}.c)
-        target_link_libraries(${basename} pastix ${NEUTRAL_PRECISION}matrix_driver)
+        target_link_libraries(${basename} pastix ${NEUTRAL_PRECISION}matrix_driver mpi-c)
         target_compile_definitions(${basename} PRIVATE NODESIZE=8 ${COMMON_DEFINITIONS})
         target_include_directories(${basename} PRIVATE
             ${PASTIX_INCLUDE_DIR_PREC}
@@ -111,4 +111,4 @@ if (BUILD_TESTS)
         ADDTEST_MPI(pingpong_n8 16)
         ADDTEST_MPI(pingpong_n8e ${NODESIZE2})
     endif()
-endif()
\ No newline at end of file
+endif()
